apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dextinity-api.fullname" . }}-test-connection"
  labels:
    {{- include "dextinity-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dextinity-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
